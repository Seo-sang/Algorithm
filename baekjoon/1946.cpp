#include <iostream>

using namespace std;
const int MN = 100001;
pair<int,int> arr[MN];

int main() {
    int T; cin >> T;
    while(T--) {
        memset(arr, 0, sizeof(arr));
        int N; cin >> N;
        for(int i = 0; i < N; i++)
            cin >> arr[i].first >> arr[i].second;
        
        
    }
}