#include <bits/stdc++.h>

using namespace std;
const int MN = 100001;
int arr[MN];
int sorted[MN];

void divNconq(int node, int start, int end) {
    if(star)
}

int main() {
    int N, M; cin >> N >> M;
    for(int i = 0; i < N; i++)
        cin >> arr[i];
}