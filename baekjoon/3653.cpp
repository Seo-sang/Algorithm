#include <bits/stdc++.h>

using namespace std;

const int MN = 100001;
int arr[MN];

int main() {
    int T; cin >> T;
    while(T--) {
        int N, M; cin >> N >> M;
        for(int i = 0; i < N; i++)
            arr[i] = i + 1;
        while(M--) {
            int a; cin >> a;
        }
    }
}