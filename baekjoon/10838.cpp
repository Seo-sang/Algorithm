#include <bits.stdc++.h>

using namespace std;

const int MN = 100001;

int main() {
    int N, K; cin >> N >> K;
    while(K--) {
        int r, a, b, c; cin >> r;
        if(r == 1) {
            cin >> a >> b >> c;
        }
        else if(r == 2) {

        }
        else {

        }
    }
}